books-rest-api
A simple REST API to manage a list of books using Node.js and Express.js.

🧩 Objective
Build a simple REST API to perform CRUD operations (Create, Read, Update, Delete) on books, stored in memory.

🛠️ Tools Used

VS Code
Node.js
Express.js

Postman (for API testing)

🎯 Features

GET /books – http://localhost:3000/books
POST /books – (http://localhost:3000/books)
PUT /books/:id – (http://localhost:3000/books/1)
DELETE /books/:id – (http://localhost:3000/books/2)

In-memory storage (no database required)

📱 How to Run
Clone or download the repository.
Open the folder in VS Code.
Install dependencies:
npm install
Start the server:
node server.js

Open Postman or any API client to test endpoints:
http://localhost:3000/books

Khushi Shah
Task – Create a REST API to manage books using Node.js & Express.js
